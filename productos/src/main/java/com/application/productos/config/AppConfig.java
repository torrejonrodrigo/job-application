package com.application.productos.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // No need for manual DataSource or EntityManagerFactory configuration
}